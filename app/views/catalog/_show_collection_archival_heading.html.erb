<%= image_tag collection_representative_image_path(document) %>

<%= link_to t('cho.collection.heading.browse'), search_archival_collection_resources_path(params[:id]) %>
<%= link_to t('cho.collection.heading.finding_aid'), root_path %>
